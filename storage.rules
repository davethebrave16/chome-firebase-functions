rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Chiunque può leggere (es. download URL)
      allow read;
      // Solo gli utenti loggati possono scrivere
      allow write: if request.auth != null;
    }
  }
}